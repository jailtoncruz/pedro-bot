import { SlashCommandCallback } from "../core/interfaces/slash-command";
import { getChannel } from "./get-channel";

export const Play: SlashCommandCallback = async (client, interaction, options) => {
	console.log('play')
};
